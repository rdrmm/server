spring:
  application:
    name: hbw
  config:
    import: optional:configserver:http://config-server:8888
  data:
    redis:
      host: redis
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082